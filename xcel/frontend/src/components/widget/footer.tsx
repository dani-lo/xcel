import React from 'react'

import styled from 'styled-components'

const StyledAppFooter = styled.div`
  background: var(--black);
  padding: 5em;
  margin-top: 7em;
`

export const AppFooter = () => {
  return <StyledAppFooter />
}